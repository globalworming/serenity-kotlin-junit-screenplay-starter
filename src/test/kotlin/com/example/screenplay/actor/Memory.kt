package com.example.screenplay.actor

class Memory {
  companion object {
    val MAILOSAUR_SERVER = "mailosaur server"
    val MAILOSAUR_DOMAIN = "mailosaur domain"
    const val AUTH_COOKIE_VALUE = "auth cookie"
  }
}
